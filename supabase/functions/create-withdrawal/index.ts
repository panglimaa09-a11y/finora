import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const auth=req.headers.get('Authorization')||'';const userClient=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}});const {data:{user}}=await userClient.auth.getUser();if(!user)throw new Error('Unauthorized');const {amount,bank_code,account_number,account_name}=await req.json();const {data,error}=await userClient.rpc('create_withdrawal',{p_amount:amount,p_bank_code:bank_code,p_account_number:account_number,p_account_name:account_name});if(error)throw error;return new Response(JSON.stringify({withdrawal_id:data,message:'Penarikan dibuat dan masuk antrean payout provider.'}),{headers:{...cors,'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:String(e)}),{status:400,headers:{...cors,'Content-Type':'application/json'}})}});
