import './style.css'
import './app-shell.css'
import { createClient } from '@supabase/supabase-js'
import { renderDapin, openDapinModal, handleDapinAction, setDapinView, deleteMember } from './dapin.js'

const supabaseUrl=import.meta.env.VITE_SUPABASE_URL
const supabaseAnonKey=import.meta.env.VITE_SUPABASE_ANON_KEY
const app=document.getElementById('app')
const ADMIN_EMAILS=['panglimaa09@gmail.com','deavani1705@gmail.com']
const configured=Boolean(supabaseUrl&&supabaseAnonKey)
export const supabase=configured?createClient(supabaseUrl,supabaseAnonKey):null
const money=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0))
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))
const safeUrl=u=>{try{const x=new URL(u);return ['http:','https:'].includes(x.protocol)?x.toString():''}catch{return ''}}
const isAdmin=u=>ADMIN_EMAILS.includes(String(u?.email||'').toLowerCase())
let state={user:null,wallet:null,transactions:[],view:'dashboard',loading:true,error:'',modal:null,sidebar:false}
function setup(){app.innerHTML=`<main class="auth"><section class="auth-card"><div class="brand">FINORA</div><div class="eyebrow">SETUP REQUIRED</div><h1>Hubungkan FINORA ke Supabase.</h1><p class="muted">Frontend berhasil dimuat. Environment Supabase belum tersedia pada deployment ini.</p><div class="notice"><strong>Required</strong><br><code>VITE_SUPABASE_URL</code><br><code>VITE_SUPABASE_ANON_KEY</code></div></section><section class="auth-art"><div class="orb orb-a"></div><div class="orb orb-b"></div><div class="art-copy"><span>FINORA PLATFORM</span><h2>Satu login untuk Wallet dan DAPIN.</h2></div></section></main>`}
function auth(){app.innerHTML=`<main class="auth"><section class="auth-card"><div class="brand">FINORA</div><div class="eyebrow">DIGITAL FINANCE PLATFORM</div><h1>Satu login.<br>Semua layanan.</h1><p class="muted">Masuk ke FINORA untuk mengakses Wallet dan DAPIN tanpa login ulang.</p><div class="oauths"><button data-provider="google">Continue with Google</button><button data-provider="facebook">Continue with Facebook</button><button data-provider="apple">Continue with Apple</button></div>${state.error?`<div class="notice">${esc(state.error)}</div>`:''}<p class="tiny">Autentikasi dan session dikelola oleh Supabase Auth.</p></section><section class="auth-art"><div class="orb orb-a"></div><div class="orb orb-b"></div><div class="art-copy"><span>FINORA × DAPIN</span><h2>Finance infrastructure for your daily operations.</h2></div></section></main>`}
async function loadWallet(){if(!state.user||!supabase)return;const w=await supabase.from('wallets').select('*').eq('user_id',state.user.id).maybeSingle();state.wallet=w.data||null;if(w.error)state.error=w.error.message;const t=await supabase.from('wallet_transactions').select('*').eq('user_id',state.user.id).order('created_at',{ascending:false}).limit(30);state.transactions=t.data||[];if(t.error)state.error=state.error||t.error.message}
function navItem(id,label,icon){return `<button class="side-item ${state.view===id?'active':''}" data-view="${id}"><span>${icon}</span><b>${label}</b></button>`}
function layout(content){const admin=isAdmin(state.user);return `<div class="app-shell"><aside class="sidebar ${state.sidebar?'open':''}"><div class="side-brand"><div class="brand-mark">F</div><div><strong>FINORA</strong><small>Financial Platform</small></div><button class="side-collapse" data-sidebar>☰</button></div><div class="side-section"><small>OVERVIEW</small>${navItem('dashboard','Dashboard','⌂')}</div><div class="side-section"><small>WALLET</small>${navItem('topup','Top Up','＋')}${navItem('transactions','Transaksi','↔')}${navItem('withdraw','Tarik Dana','↗')}</div><div class="side-section"><small>APLIKASI</small>${navItem('dapin','DAPIN Balongbendo','D')}</div>${admin?`<div class="side-section"><small>ADMIN</small>${navItem('admin','Admin Center','A')}</div>`:''}<div class="side-bottom"><div class="session-user"><div class="avatar">${esc((state.user.email||'U')[0].toUpperCase())}</div><span><strong>${esc(state.user.user_metadata?.full_name||state.user.email?.split('@')[0]||'User')}</strong><small>${admin?'Administrator':'Member'}</small></span></div><button class="side-item logout" id="logout"><span>↪</span><b>Keluar</b></button></div></aside><div class="side-overlay" data-sidebar></div><main class="main-shell"><header class="main-header"><button class="mobile-menu" data-sidebar>☰</button><div><span class="header-kicker">${state.view==='dapin'?'DAPIN BALONGBENDO':state.view==='admin'?'ADMIN CENTER':'FINORA'}</span><strong>${pageTitle()}</strong></div><div class="header-actions"><span class="online">● Online</span><button class="icon-btn">⌕</button></div></header><div class="page">${content}</div></main>${state.modal||''}</div>`}
function pageTitle(){return ({dashboard:'Dashboard',topup:'Top Up Wallet',transactions:'Riwayat Transaksi',withdraw:'Tarik Dana',dapin:'Simpan Pinjam',admin:'Administration'})[state.view]||'FINORA'}
function dashboard(){const w=state.wallet||{};return `<div class="page-head"><div><span class="eyebrow">FINORA WALLET</span><h1>Dashboard</h1><p>Ringkasan saldo dan aktivitas akun Anda.</p></div><div class="head-buttons"><button class="secondary" data-view="dapin">Buka DAPIN →</button><button class="primary" data-view="topup">＋ Top Up</button></div></div><div class="wallet-hero"><div><span>AVAILABLE BALANCE</span><strong>${money(w.available_balance)}</strong><small>Pending ${money(w.pending_balance)}</small></div><div class="hero-meta"><span>WALLET ID</span><b>${esc(w.id||'Belum tersedia')}</b><i>● Active</i></div></div><div class="metric-grid"><article><span>Total transaksi</span><strong>${state.transactions.length}</strong><small>30 transaksi terakhir</small></article><article><span>Status akun</span><strong>Verified</strong><small>Supabase Auth</small></article><article><span>Security</span><strong>Protected</strong><small>RLS + server ledger</small></article></div><section class="panel"><div class="panel-head"><div><span class="eyebrow">RECENT ACTIVITY</span><h2>Aktivitas terbaru</h2></div><button class="link" id="refresh">Refresh</button></div>${txList()}</section>`}
function txList(){if(!state.transactions.length)return `<div class="empty">Belum ada transaksi wallet.</div>`;return `<div class="transaction-list">${state.transactions.map(t=>{const incoming=['topup','transfer_in','refund','adjustment'].includes(t.type);return `<div class="transaction-row"><div class="tx-icon ${incoming?'in':'out'}">${incoming?'↓':'↑'}</div><div class="tx-info"><strong>${esc(t.description||t.type||'Transaksi')}</strong><small>${new Date(t.created_at).toLocaleString('id-ID')}</small></div><b class="${incoming?'credit':'debit'}">${incoming?'+':'-'}${money(t.amount)}</b></div>`}).join('')}</div>`}
function topup(){return `<div class="page-head"><div><span class="eyebrow">WALLET</span><h1>Top Up</h1><p>Tambah saldo melalui payment provider yang aktif.</p></div></div><section class="panel form-panel"><div class="panel-head"><div><h2>Buat pembayaran</h2><p>Saldo hanya bertambah setelah webhook settlement terverifikasi.</p></div><span class="secure">✓ Protected</span></div><div class="form-grid"><label>Nominal<input id="topupAmount" type="number" min="10000" step="1000" placeholder="100000"></label><label>Metode<select id="topupMethod"><option value="qris">QRIS</option><option value="va">Virtual Account</option><option value="bank_transfer">Bank Transfer</option></select></label></div><button class="primary wide" id="submitTopup">Buat Pembayaran</button></section>`}
function transactions(){return `<div class="page-head"><div><span class="eyebrow">WALLET LEDGER</span><h1>Transaksi</h1><p>Riwayat transaksi wallet FINORA.</p></div><button class="secondary" id="refresh">↻ Refresh</button></div><section class="panel">${txList()}</section>`}
function withdraw(){return `<div class="page-head"><div><span class="eyebrow">PAYOUT</span><h1>Tarik Dana</h1><p>Ajukan penarikan dari saldo wallet.</p></div></div><section class="panel form-panel"><div class="notice">Fitur payout mengikuti status provider dan aktivasi metode pembayaran Production.</div><div class="form-grid"><label>Nominal<input type="number" min="10000" placeholder="100000"></label><label>Tujuan<select><option>Bank</option><option>Dompet Digital</option></select></label></div><button class="primary wide" disabled>Ajukan Penarikan</button></section>`}
function admin(){return `<div class="page-head"><div><span class="eyebrow">ADMIN CENTER</span><h1>Administration</h1><p>Kontrol akses dan monitoring platform.</p></div><span class="admin-badge">ADMIN VERIFIED</span></div><div class="metric-grid"><article><span>Administrator</span><strong>2</strong><small>Authorized accounts</small></article><article><span>Payment</span><strong>Online</strong><small>Webhook endpoint active</small></article><article><span>DAPIN</span><strong>Connected</strong><small>Single session</small></article></div><section class="panel"><div class="panel-head"><div><span class="eyebrow">AUTHORIZED USERS</span><h2>Admin Accounts</h2></div></div><div class="admin-users"><div><span class="avatar">P</span><strong>panglimaa09@gmail.com</strong><em>ADMIN</em></div><div><span class="avatar">D</span><strong>deavani1705@gmail.com</strong><em>ADMIN</em></div></div></section>`}
function appView(){const content=state.view==='dapin'?renderDapin():state.view==='admin'&&isAdmin(state.user)?admin():state.view==='topup'?topup():state.view==='transactions'?transactions():state.view==='withdraw'?withdraw():dashboard();return layout(content)}
function render(){if(!configured){setup();return}if(state.loading){app.innerHTML='<div class="loading">Memuat FINORA…</div>';return}if(!state.user){auth();return}app.innerHTML=appView()}
async function signIn(provider){state.error='';render();const {error}=await supabase.auth.signInWithOAuth({provider,options:{redirectTo:location.origin}});if(error){state.error=error.message;render()}}
async function invoke(name,body){const {data:{session}}=await supabase.auth.getSession();if(!session)throw Error('Session login berakhir.');const {data,error}=await supabase.functions.invoke(name,{body,headers:{Authorization:`Bearer ${session.access_token}`}});if(error)throw Error(error.message||`Edge Function ${name} gagal`);if(data?.error)throw Error(data.error);return data}
async function doTopup(){const amount=Number(document.getElementById('topupAmount')?.value),method=document.getElementById('topupMethod')?.value;if(!Number.isInteger(amount)||amount<10000)return alert('Minimum top up Rp10.000.');try{const d=await invoke('provider-create-topup',{amount,method});const url=safeUrl(d.payment_url);state.modal=`<div class="modal-wrap"><div class="modal"><h2>Pembayaran dibuat</h2><p>Top Up ${money(amount)} menunggu pembayaran.</p>${url?`<a class="primary wide" href="${esc(url)}" target="_blank" rel="noopener">Lanjut ke Pembayaran</a>`:''}<button class="secondary wide" id="closeModal">Tutup</button></div></div>`;await loadWallet();render()}catch(e){alert(e.message)}}
function handleDapinForm(form){try{const fd=Object.fromEntries(new FormData(form).entries());handleDapinAction(form.dataset.dapinForm,fd);state.modal=null;render()}catch(e){alert(e.message)}}
document.addEventListener('click',async e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.provider)return signIn(b.dataset.provider);if(b.dataset.sidebar){state.sidebar=!state.sidebar;render();return}if(b.id==='logout'){await supabase.auth.signOut();return}if(b.dataset.view){state.view=b.dataset.view;state.sidebar=false;state.error='';if(['dashboard','topup','transactions','withdraw'].includes(state.view))await loadWallet();render();return}if(b.id==='refresh'){state.error='';await loadWallet();render();return}if(b.id==='submitTopup')return doTopup();if(b.id==='closeModal'){state.modal=null;render();return}if(b.dataset.dapinClose!==undefined){state.modal=null;render();return}if(state.view==='dapin'){if(b.dataset.dapinView){setDapinView(b.dataset.dapinView);state.modal=null;render();return}if(b.dataset.dapinAction&&b.dataset.dapinAction!=='delete-member'){state.modal=openDapinModal(b.dataset.dapinAction);render();return}if(b.dataset.dapinAction==='delete-member'&&b.dataset.id){if(confirm('Hapus anggota ini?')){deleteMember(b.dataset.id);render()}return}}})
document.addEventListener('submit',e=>{if(e.target.matches('[data-dapin-form]')){e.preventDefault();handleDapinForm(e.target)}})
if(configured){supabase.auth.onAuthStateChange(async(_event,session)=>{state.user=session?.user||null;if(state.user)await loadWallet();else{state.wallet=null;state.transactions=[]}state.loading=false;state.view='dashboard';render()});(async()=>{const {data:{session}}=await supabase.auth.getSession();state.user=session?.user||null;if(state.user)await loadWallet();state.loading=false;render()})()}else{state.loading=false;render()}
