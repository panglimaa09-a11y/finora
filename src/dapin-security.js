const FACE_KEY='finora_dapin_face_verification_v1'
const load=()=>{try{return JSON.parse(localStorage.getItem(FACE_KEY))||{status:'not_started'}}catch{return {status:'not_started'}}}
const save=x=>localStorage.setItem(FACE_KEY,JSON.stringify(x))
export function faceVerificationCard(){const v=load();const label=v.status==='verified'?'Terverifikasi':v.status==='captured'?'Dokumen wajah siap':'Belum diverifikasi';const tone=v.status==='verified'?'success':v.status==='captured'?'pending':'idle';return `<section class="security-card"><div class="security-card-head"><div><span class="eyebrow">KEAMANAN AKUN</span><h2>Verifikasi wajah</h2><p>Pastikan pengajuan dilakukan oleh pemilik akun. Verifikasi wajah adalah pemeriksaan identitas tambahan dan bukan penentu kelayakan pinjaman.</p></div><span class="security-status ${tone}">${label}</span></div><div class="security-checks"><div><b>✓</b><span><strong>KTP</strong><small>Identitas harus diverifikasi terlebih dahulu</small></span></div><div><b>◉</b><span><strong>Liveness</strong><small>Pastikan Anda orang nyata di depan kamera</small></span></div><div><b>◇</b><span><strong>Face match</strong><small>Hasil akhir diproses oleh layanan KYC yang dikonfigurasi admin</small></span></div></div><button class="security-primary" id="start-face">${v.status==='captured'?'Ulangi Verifikasi':'Mulai Verifikasi Wajah'} →</button><small class="security-privacy">🔒 Kamera hanya digunakan saat verifikasi berlangsung. Jangan menyimpan foto wajah di localStorage untuk produksi.</small></section>`}
export function bindFaceVerification(){document.querySelector('#start-face')?.addEventListener('click',openFaceModal)}
function openFaceModal(){const wrap=document.createElement('div');wrap.className='face-modal-wrap';wrap.innerHTML=`<div class="face-modal"><button class="face-close" id="face-close">×</button><span class="eyebrow">VERIFIKASI WAJAH</span><h2>Posisikan wajah Anda</h2><p>Izinkan kamera lalu ikuti instruksi. Jangan gunakan foto atau video sebagai pengganti wajah.</p><div class="camera-stage"><video id="face-video" autoplay playsinline muted></video><div class="face-guide"><span></span></div><div class="camera-message" id="camera-message">Menyiapkan kamera…</div></div><div class="face-actions"><button class="security-secondary" id="face-cancel">Batal</button><button class="security-primary" id="face-capture" disabled>Konfirmasi Kehadiran</button></div><small class="security-privacy">Untuk produksi, hasil ini harus dikirim ke provider liveness/KYC dan diverifikasi di server.</small></div>`;document.body.appendChild(wrap);let stream=null;const video=wrap.querySelector('#face-video'),capture=wrap.querySelector('#face-capture'),message=wrap.querySelector('#camera-message');const close=()=>{stream?.getTracks().forEach(t=>t.stop());wrap.remove()};wrap.querySelector('#face-close').onclick=close;wrap.querySelector('#face-cancel').onclick=close;(async()=>{try{if(!navigator.mediaDevices?.getUserMedia)throw Error('Browser tidak mendukung akses kamera.');stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'user',width:{ideal:720},height:{ideal:720}},audio:false});video.srcObject=stream;message.textContent='Kamera aktif. Pastikan wajah berada di dalam lingkaran.';capture.disabled=false}catch(e){message.textContent='Kamera tidak dapat digunakan. Pastikan izin kamera diberikan.'}})();capture.onclick=()=>{save({status:'captured',captured_at:new Date().toISOString(),provider_status:'pending_server_kyc'});message.textContent='✓ Kehadiran terdeteksi. Verifikasi server diperlukan untuk status terverifikasi.';capture.disabled=true;setTimeout(close,900);setTimeout(()=>window.dispatchEvent(new CustomEvent('dapin-security-updated')),950)}}
