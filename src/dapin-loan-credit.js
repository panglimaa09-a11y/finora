const KEY='finora_dapin_v2'
const CREDIT_KEY='finora_dapin_credit_v1'
const rupiah=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(Number(n||0))
const read=k=>{try{return JSON.parse(localStorage.getItem(k)||'{}')}catch{return {}}}
const currentMember=()=>{const d=read(KEY),email=document.querySelector('.session-user strong')?.textContent?.trim()||'';return (d.members||[]).find(m=>m.email===email)}
function apply(){const page=document.querySelector('.loan-app-page');if(!page||page.dataset.creditBound)return;page.dataset.creditBound='1';const m=currentMember();if(!m)return;const c=read(CREDIT_KEY)[m.id]||{limit:3000000,maxTenor:12,risk:'Belum dinilai'};const hero=page.querySelector('.loan-hero');if(hero){const box=document.createElement('div');box.className='loan-credit-limit';box.innerHTML=`<span>PLAFON ANDA</span><strong>${rupiah(c.limit)}</strong><small>Tenor maksimum ${c.maxTenor} bulan • ${c.risk}</small>`;hero.appendChild(box)}const amount=document.querySelector('#loan-amount'),tenor=document.querySelector('#loan-tenor'),next=document.querySelector('#continue-loan');if(amount){[...amount.options].forEach(o=>{if(Number(o.value)>c.limit)o.disabled=true});if(Number(amount.value)>c.limit){const first=[...amount.options].find(o=>!o.disabled);if(first)amount.value=first.value}amount.addEventListener('change',()=>{if(Number(amount.value)>c.limit){amount.value=String(c.limit);alert(`Nominal maksimal Anda ${rupiah(c.limit)}.`)}})}if(tenor){[...tenor.options].forEach(o=>{if(Number(o.value)>c.maxTenor)o.disabled=true});if(Number(tenor.value)>c.maxTenor){const first=[...tenor.options].find(o=>!o.disabled);if(first)tenor.value=first.value}tenor.addEventListener('change',()=>{if(Number(tenor.value)>c.maxTenor){tenor.value=String(c.maxTenor);alert(`Tenor maksimal Anda ${c.maxTenor} bulan.`)}})}next?.addEventListener('click',e=>{if(Number(amount?.value||0)>c.limit){e.preventDefault();alert(`Pengajuan melebihi plafon Anda ${rupiah(c.limit)}.`)}if(Number(tenor?.value||0)>c.maxTenor){e.preventDefault();alert(`Tenor maksimal Anda ${c.maxTenor} bulan.`)}})}
const observer=new MutationObserver(apply);observer.observe(document.documentElement,{childList:true,subtree:true});apply()
